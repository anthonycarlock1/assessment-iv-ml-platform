apiVersion: v1
kind: Service
metadata:
  name: recommendations-service
  namespace: fraud-team
spec:
  selector:
    app: recommendations-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
