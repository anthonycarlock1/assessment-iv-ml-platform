apiVersion: v1
kind: Service
metadata:
  name: forecasting-service
  namespace: fraud-team
spec:
  selector:
    app: forecasting-service
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
